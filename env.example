# Copy this file to .env.local and fill in values as needed

# Database (required)
NEXT_PUBLIC_DATABASE_URL=postgresql://username:password@host:port/database

# Client-side
NEXT_PUBLIC_API_BASE=

# Server-side (keep secrets only in .env.local /.env.production)
DIGEST_CRON_SECRET=
EMAIL_PROVIDER_API_KEY=

# AI Provider (optional - for enhanced summaries)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Optional: override data directory (defaults to ./data)
DATA_DIR= 