#!/bin/sh
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact --config .gitleaks.toml
else
  echo "gitleaks not installed; skipping local secret scan (CI will still enforce it). Install: https://github.com/gitleaks/gitleaks"
fi
npx --no -- lint-staged
